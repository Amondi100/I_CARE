import 'package:flutter/material.dart';

class ListOfTherapists extends StatelessWidget {
  const ListOfTherapists({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
