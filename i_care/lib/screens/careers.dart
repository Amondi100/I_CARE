import 'package:flutter/material.dart';

class JoinNetwork extends StatelessWidget {
  const JoinNetwork({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
